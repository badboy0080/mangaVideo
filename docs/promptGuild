# 提示词与阶段指南（Prompt Guild）

本文说明完整视频生产各阶段逻辑、故事板与媒体生成要点，以及提示词书写规范。

---

## 1. 完整视频的阶段逻辑和依赖关系

### 1.1 阶段流程

1. **资源与纲要**：一句话总结用户上传的剧本文件（图片 / PDF / 文本）→ `resource_prepare_and_analyze`。
2. **视频规格**：编写 `Final_Video_Spec.md`（标题、类型、画幅、时长、视觉风格、语言、模型偏好）→ `text_editor`。
3. **故事板**：生成与第 1 步相符的故事板（关键元素、镜头列表、音频层）→ `storyboard_designer`。
4. **元素设置**：
   - 若用户已上传元素资源（如角色图、引用角色），直接绑定为对应 `key_elements` 资产。
   - 否则为所有元素生成图像 → `media_generator`。
5. **镜头视频**：逐镜头生成最终视频，严格参考该镜头的元素图（第 4 步）→ `media_generator`。
6. **音频层**：基于故事板生成全部 `audio_layers` → `media_generator`。
7. **成片装配**：资源齐套后最终剪辑，并引导导出 → `video_assembler`。

### 1.2 依赖关系

`2 → 1`；`3 → 1, 2`；`4 → 3`；`5 → 2, 3, 4`；`6 → 3`；`7 → 3, 4, 5, 6`。

### 1.3 用户已提供的媒体（主要影响步骤 3–6）

在补全生成内容前，通过 **`media_generator`** 将用户媒体绑定并分配到合适分镜位；避免重复生成用户已提供的内容。



---

## 2. 故事板设计

指导 AI 如何撰写故事板：包含哪些元素、如何设计镜头、如何描述画面与镜头语言。

### 2.1 脚本忠实度（最高优先级）

- 分镜中的 **对话、旁白、动作结果与场景顺序** 须与上传脚本及第 1 步分解一致。
- 仅允许 **技术性** 编辑：**拆分镜头**（≤15s）或补充 **镜头语言** 字段。
- **禁止** 虚构情节或改写台词。

### 2.2 如何设计关键元素

- 始终包含 **关键主体**（角色、物体等）、**关键地点 / 场景** 与 **关键道具**（如有）。
- **`element_id` 规划**：角色、道具、场景作为独立实体时，各分配唯一 ID（例如 `[Element_Detective_Li]`、`[Element_Boss_Zhao]`、`[Element_Observation_Room]`、`[Element_Chief_Office]`）。
- **描述写法**：
  - **角色**：若有多状态 / 外观（服装、年龄等），在描述中分条写清；包含 **声音 / 音色**，便于后续音频对齐。
  - **关键地点 / 场景**：写清重要物体位置与朝向，尤其是道具区或主要动作区。
  - **用户提供的资产**：若用户指定参考媒体为关键元素，描述 **须与资产一致**，不得与所见所闻矛盾。

### 2.3 如何设计镜头

- 每镜须含：**场景（元素）**、**故事与表演（具体台词）**、**镜头语言**（景别、角度、运动）。
- **视觉与事实** 须符合脚本；脚本未写镜头覆盖时，可从类型与基调 **专业推断** 构图与运动，但 **不得** 改情节事实或对白。
- **倾向长镜头 + 内部剪辑**（如单镜 10–15s）：在镜 **内** 做剪切，而非拆成大量短镜；剪切节奏跟故事走。**单镜不超过 15s**（模型上限）。
- **镜内多剪切**：在场景 / 设定之后，按剪切顺序写故事与镜头语言，用明确时间与剪切标记，例如：  
  `Shot1 (0–4s): [场景], [角色] 做 X，特写。Shot2 切至 (4–8s): … Shot3 切至 (8–12s): …`
- **每镜描述须含**：
  - **场景**：引用地点 / 布景（场景元素 ID，如 `[Element_Office_Noir]`）。
  - **故事内容**：角色与关键物体动作；**写清台词**（对白或独白 / 画外）；用元素 ID 指角色。
  - **镜头语言**：景别、角度、运动。

### 2.4 如何设计独立音频

- **BGM**：至少一条全局轨；风格与节奏对齐参考或《最终视频规范》；若参考中有明确节奏 / 情绪变化，可拆 BGM 段并定义范围（各有 `audio_id` 与范围）。
- **旁白**：若参考含旁白 / 对白，在 `audio_layers`（类型 `narration`）中设计，含内容、声线 / 语调与时间范围；若有 `voice_reference` 资产，在 `layout_instruction` 或备注中链接。

---

## 3. 媒体生成

指导 AI 如何生成图 / 视频 / 音频：模型选择、参考素材与输出设置。

### 3.1 元素生成

- **图像**：用户已为元素指定资产（产品图、角色图等）则 **直接使用**，勿再生成。否则角色 / 场景 / 道具用 **TextToImage**（或项目约定）。同一角色多状态可用 **ImageToImage** 参考首图保一致。按项目约定分辨率与模型（如 2K）；角色可多视图以保跨镜一致，每视图单独 `asset_id` 并分别生成。
  - 推荐模型：**Nano Banana 2（Gemini 3.1 Flash Image）**，分辨率 **2K**。
  - **角色**：每张横向 16:9，画面从左到右含 **半身特写 + 全身三视图（正、侧、背）**，便于 Seedance 读取身份与服装。
- **最终镜头视频**：使用 **MultiModalToVideo**。推荐 **Seedance 2.0**，**720p**。参考该镜 **元素图**。若有生成或用户上传的 **音色参考**，与元素图一并作为 Seedance 参考输入。

### 3.2 旁白与 BGM

- **旁白**：内容、声线、语调与时间 **严格按分镜**；若有 `voice_reference`，生成时对齐。
- **BGM**：风格与节奏对齐最终规范 / 参考；时长与范围按分镜。

---

## 4. 提示词写法

指导 AI 在生成媒体时如何写提示词（图、视频及增效要点）。

### 4.1 语言优先级（最高）

当用户使用 **中文输入** 或 **中文语言环境** 时，**提示词主体用中文书写**；其中 **旁白 / 对白内容** 须严格遵循 `Final_Video_Spec` 中的 **Output Language**。

### 4.2 图像生成提示词（TextToImage / ImageToImage）

#### 通用原则

- 提示词不只是「画面有什么」，而是像导演 + 调色师在 Brief 团队：用自然语言写清 **主体 + 行为 + 环境**，用短语写 **美学**（风格、色彩、光影、构图）。拒绝文学化动机描写；不要写屏外或不可见信息；只写 **可见** 物。
- 对所有图像（尤其角色），在目标风格下贯彻 **电影级提示 6 条**，织成一段流畅描述；**不要**输出成带标签的小标题列表：
  1. **风格术语**：专业艺术 / 电影术语 + 风格锚点（如 `Neo-Noir style, David Fincher style, inspired by Se7en`），避免空泛 `Neo-Noir`。
  2. **构图与镜头**：电影化构图（如肩后镜头、荷兰角）与景别（特写、中景等）。
  3. **光影**：主光 + 「负补光」增强对比（如明暗对比、戏剧性阴影、面部结构阴影）。
  4. **调色**：克制的好莱坞式调色（如大面积单一主色、少暖填充等）；除非用户要求，避免红蓝霓虹对撞；单主色区域约 **90%** 克制。
  5. **渲染质量**：精细渲染、细节、柔焦 / 质感等。
  6. **氛围与潜台词**：微表情、戏剧性瞬间；无生命物体写 **状态** 而非心理动机。
- **条件性**：画面内可读文字须写清，**引号内为需渲染的精确文案**，例如：背景霓虹牌写着 `"DANGER"`。

> **说明**：原文档此节曾写「将这 6 条写成流畅英语」；与上文「中文环境用中文」并存时，以 **§4.1 语言优先级** 为准（中文环境下优先中文表述，术语可保留英文专名）。

#### 目标为 `key_element`（关键元素）时

- 结构围绕：**主体与身份**（类型、角色特征、年龄、体态、显著特征、声音等）→ **特征细节**（眉眼妆造、服装配饰、道具形态材质、场景情感潜台词等）。为保画面干净，**不在画面内生成无关大字**。

#### 目标为关键帧（起幅 / 落幅 / 高潮帧）时

- 从该镜 `Description` 锁定 **瞬时**：起幅 / 落幅 / 戏剧高峰；若描述不清，在脑中走一遍「主体 + 机位 + 环境」再补细节。**只描述那一帧的静态可见内容**：
  - **起幅**：动作前姿态、注视、环境初态、机位初构图。
  - **落幅**：动作结束姿态 / 表情、环境变化后状态、机位落点。
  - **高潮帧**：冲击力最强的一瞬，细节拉满，可单独成静帧。

### 4.3 视频生成提示词

- 故事板 / 分镜 / 脚本中出现 **角色对白** 时，视频 prompt **必须写出对白**，并按 Seedance 要求用 **`{…}`** 格式。
- 若上传 **配音 / 音色参考** 且需跨镜一致，将 **音色参考** 作为 Seedance 2.0 参考项之一，并写明 **哪一角色对应哪条参考音频**。

#### Seedance 叙事顺序（摄像机 → 主体 → 空间 → 音频）

1. **摄像机**：运动或剪辑变化（如 `static → push-in`、`cut to new angle`、`orbit`、`pan`）。
2. **主体**：动作与表情节拍。
3. **空间**：主体或机位相对空间的变化、背景运动、纵深 / 布局变化。
4. **音频**：对白（如有）、音效；若无音乐需在文内说明（见下）。

#### 动作细节

- 写到 **具体部位**（手、腿、头、肩等）与 **幅度 / 速度 / 强度**（如缓慢抬手、猛转头、蹬地发力）。
- 多节拍时按时间顺序：**主动作 → 次动作**。

#### 多节拍与「镜头 1 / 镜头 2」

- 长提示词中可用 **镜头 1、镜头 2、镜头 3** 标 **机位或叙事节拍** 序列。
- **不要用精确秒表**（如「0–3s」「在 2s 时」）；模型难以严格执行。只写 **先后顺序**，不要写成日程表式分镜表。

#### 可选润色

- 真人电影感时可加 **轻微手持感 / 胶片颗粒**，次于上述四层顺序。

#### Seedance 2.0 包装约定

- **音乐**：`(...)`，例如 `(fast-paced rock playing in the background)`。
- **音效**：`<...>`，例如 `<distant dog barking>`。
- **口头对白**：`{...}`；非项目语言时在前加语言说明，如 *Says in Japanese:* `{こんにちは}`。花括号内 **只保留对白字**，不要故事板里的引号。
- **屏显 / 章节 / 字幕文案**：`【...】`，例如 `【Chapter One: Departure】`。

#### 负面约束（建议固定带上）

- 若分镜规定该镜有 **独立旁白轨**（画外音，非角色口型对白），**不要把旁白全文写进视频 prompt**，避免与画面内对白冲突。
- 为减少乱配 BGM，文内常带 **`no music`**（按项目需要）。
- 字幕后期再加，文内常带 **`no subtitles`**。
