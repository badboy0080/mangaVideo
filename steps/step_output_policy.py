"""
Step 01～06 产出约定（供各 step 模块引用说明）

- **LLM 步骤（1、2、4 单资产、6 润色）**：写入 JSON/DB 的**正文**必须来自模型返回；失败时保持**空字符串**，禁止本地模板句、占位分镜、拼接式「叙事回退」。
- **整理步骤（3、4 元数据、5、6 元数据）**：只做解析、合并、路径映射、时长夹紧等；不向用户可见的 prompt 字段**发明**文案。
- **Step 3**：从 Step 2 的 LLM 分镜中抽取 `@` 列表；`description` 仅为脚本中的原文摘录，不调用模型。
"""

LLM_BODY_STEPS = frozenset({1, 2, 4, 6})
